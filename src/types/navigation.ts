// Define interface for navigation items
export interface NavItem {
  label: string;
  href: string;
}

// Define interface for social links (used in footer)
export interface SocialLink {
  platform: string;
  url: string;
  icon: string;
}
